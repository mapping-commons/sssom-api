version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: docker-api/Dockerfile
    command: uvicorn app.main:app --host 0.0.0.0
    volumes:
      - ./src:/code/app
      - .env:/code/.env
    ports:
      - 8008:8000
    depends_on:
      - triplestore
      - upload-triplestore
  triplestore:
    image: yyz1989/rdf4j:latest
    environment:
      - JVM_PARAMS=-Xms1G -Xmx5G
      - RDF4J_DATA=/data 
    ports:
      - 8080:8080
  upload-triplestore:
    build:
      context: .
      dockerfile: docker-upload-triplestore/Dockerfile
    depends_on:
      - triplestore
    links:
      - triplestore
    environment:
      - SERVER=http://triplestore:8080
    volumes:
      - ./data:/data